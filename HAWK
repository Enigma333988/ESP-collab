-- HAWK Bananas-only mini UI

local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

local state = {
    bananaEnabled = false,
    draggingWindow = false,
}

local ui = {}

local function getCharacter(player)
    player = player or LocalPlayer
    if player.Character then
        return player.Character
    end

    local playersFolder = Workspace:FindFirstChild("Players")
    if playersFolder then
        return playersFolder:FindFirstChild(player.Name)
    end

    return nil
end

local function getRootPart(character)
    character = character or getCharacter(LocalPlayer)
    return character and character:FindFirstChild("HumanoidRootPart")
end

local function collectBananas3()
    local prefabs = Workspace:FindFirstChild("Prefabs")
    if not prefabs then
        return 0
    end

    local rootPart = getRootPart(getCharacter(LocalPlayer))
    if not rootPart then
        return 0
    end

    local moved = 0
    for _, obj in ipairs(prefabs:GetChildren()) do
        if obj.Name == "Bananas3" and obj:IsA("MeshPart") then
            obj.CFrame = rootPart.CFrame
            moved += 1
        end
    end

    return moved
end

local function setStatus(text)
    if ui and ui.statusBar then
        ui.statusBar.Text = "  " .. text
    end
end

-- ---------- UI ----------
local oldGui = PlayerGui:FindFirstChild("HawkUnifiedGui")
if oldGui then
    oldGui:Destroy()
end

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "HawkUnifiedGui"
screenGui.ResetOnSpawn = false
screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
screenGui.Parent = PlayerGui
ui.screenGui = screenGui

local window = Instance.new("Frame")
window.Size = UDim2.fromOffset(360, 200)
window.Position = UDim2.new(0.5, -180, 0.5, -100)
window.BackgroundColor3 = Color3.fromRGB(20, 22, 28)
window.BorderSizePixel = 0
window.Parent = screenGui

local windowCorner = Instance.new("UICorner")
windowCorner.CornerRadius = UDim.new(0, 12)
windowCorner.Parent = window

local topbar = Instance.new("Frame")
topbar.Size = UDim2.new(1, 0, 0, 40)
topbar.BackgroundColor3 = Color3.fromRGB(31, 36, 46)
topbar.BorderSizePixel = 0
topbar.Parent = window

local topCorner = Instance.new("UICorner")
topCorner.CornerRadius = UDim.new(0, 12)
topCorner.Parent = topbar

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -96, 1, 0)
title.Position = UDim2.fromOffset(12, 0)
title.BackgroundTransparency = 1
title.Font = Enum.Font.GothamBold
title.TextSize = 16
title.TextXAlignment = Enum.TextXAlignment.Left
title.TextColor3 = Color3.fromRGB(240, 245, 255)
title.Text = "HAWK | Bananas"
title.Parent = topbar

local closeBtn = Instance.new("TextButton")
closeBtn.Size = UDim2.fromOffset(72, 26)
closeBtn.Position = UDim2.new(1, -80, 0.5, -13)
closeBtn.BackgroundColor3 = Color3.fromRGB(190, 72, 72)
closeBtn.BorderSizePixel = 0
closeBtn.Font = Enum.Font.GothamBold
closeBtn.TextSize = 13
closeBtn.Text = "Закрыть"
closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
closeBtn.Parent = topbar

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(0, 8)
closeCorner.Parent = closeBtn

local bananaToggle = Instance.new("TextButton")
bananaToggle.Size = UDim2.fromOffset(220, 40)
bananaToggle.Position = UDim2.fromOffset(12, 56)
bananaToggle.BackgroundColor3 = Color3.fromRGB(176, 138, 56)
bananaToggle.BorderSizePixel = 0
bananaToggle.Font = Enum.Font.GothamSemibold
bananaToggle.TextSize = 14
bananaToggle.TextColor3 = Color3.fromRGB(255, 255, 255)
bananaToggle.Text = "Автосбор Bananas3: ВЫКЛ"
bananaToggle.Parent = window

local bananaToggleCorner = Instance.new("UICorner")
bananaToggleCorner.CornerRadius = UDim.new(0, 8)
bananaToggleCorner.Parent = bananaToggle

local bananaPulse = Instance.new("TextButton")
bananaPulse.Size = UDim2.fromOffset(112, 40)
bananaPulse.Position = UDim2.fromOffset(236, 56)
bananaPulse.BackgroundColor3 = Color3.fromRGB(100, 124, 64)
bananaPulse.BorderSizePixel = 0
bananaPulse.Font = Enum.Font.GothamSemibold
bananaPulse.TextSize = 14
bananaPulse.TextColor3 = Color3.fromRGB(255, 255, 255)
bananaPulse.Text = "Собрать"
bananaPulse.Parent = window

local bananaPulseCorner = Instance.new("UICorner")
bananaPulseCorner.CornerRadius = UDim.new(0, 8)
bananaPulseCorner.Parent = bananaPulse

local statusBar = Instance.new("TextLabel")
statusBar.Size = UDim2.new(1, -24, 0, 30)
statusBar.Position = UDim2.new(0, 12, 1, -42)
statusBar.BackgroundColor3 = Color3.fromRGB(26, 30, 39)
statusBar.BorderSizePixel = 0
statusBar.Font = Enum.Font.Gotham
statusBar.TextSize = 13
statusBar.TextColor3 = Color3.fromRGB(210, 220, 235)
statusBar.TextXAlignment = Enum.TextXAlignment.Left
statusBar.Text = "  Готово."
statusBar.Parent = window
ui.statusBar = statusBar

local statusCorner = Instance.new("UICorner")
statusCorner.CornerRadius = UDim.new(0, 8)
statusCorner.Parent = statusBar

local dragStart, startPos

topbar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        state.draggingWindow = true
        dragStart = input.Position
        startPos = window.Position
    end
end)

topbar.InputEnded:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        state.draggingWindow = false
    end
end)

bananaToggle.MouseButton1Click:Connect(function()
    state.bananaEnabled = not state.bananaEnabled
    bananaToggle.Text = state.bananaEnabled and "Автосбор Bananas3: ВКЛ" or "Автосбор Bananas3: ВЫКЛ"
    setStatus("Автосбор " .. (state.bananaEnabled and "включен" or "выключен"))
end)

bananaPulse.MouseButton1Click:Connect(function()
    local count = collectBananas3()
    setStatus("Собрано Bananas3: " .. count)
end)

closeBtn.MouseButton1Click:Connect(function()
    screenGui:Destroy()
end)

Workspace:GetPropertyChangedSignal("CurrentCamera"):Connect(function()
    if not screenGui.Parent then
        return
    end
end)

game:GetService("RunService").RenderStepped:Connect(function()
    if state.draggingWindow and dragStart and startPos then
        local delta = game:GetService("UserInputService"):GetMouseLocation() - dragStart
        window.Position = UDim2.new(
            startPos.X.Scale,
            startPos.X.Offset + delta.X,
            startPos.Y.Scale,
            startPos.Y.Offset + delta.Y
        )
    end

    if state.bananaEnabled then
        collectBananas3()
    end
end)

setStatus("Bananas-only режим")
